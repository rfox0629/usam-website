/* One component root for DOS (Phase 1 S5 / Phase 2 I18): the circle target
 * still lives in components/dos/ because loading.tsx files import it from
 * there; new code imports it from here. */
export { DosCircleTarget, dosCircleTargetSize, type DosCircleLayer, type DosCircleTargetCounts } from "@/components/dos/DosCircleTarget";
