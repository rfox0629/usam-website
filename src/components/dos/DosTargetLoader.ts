/* One component root for DOS (Phase 1 S5 / Phase 2 I18): re-export of the
 * route loading skeleton so new code has a single import path. */
export { DosTargetLoader } from "@/components/dos/DosTargetLoader";
