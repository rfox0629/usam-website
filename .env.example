# Access code required to unlock the private /partners page.
PARTNERS_ACCESS_KEY=

# Access code required to unlock the private /vision page.
# Rotate this value after each briefing to revoke previously issued sessions.
VISION_ACCESS_KEY=

# Public analytics IDs. Values are safe to expose to the browser, but do not
# include names, email addresses, prayer content, or other PII in event params.
NEXT_PUBLIC_GA_MEASUREMENT_ID=
NEXT_PUBLIC_CLARITY_PROJECT_ID=

# Dispatcher review-preview runner settings. These are for the isolated
# automation runner only; do not expose tokens to the browser.
DISPATCHER_REVIEW_PUSH_DISABLED=
DISPATCHER_REVIEW_PUSH_ENABLED=
DISPATCHER_TEST_MODE=
DISPATCHER_SINGLE_WORKER_MODE=
DISPATCHER_REVIEW_PUSH_IN_SINGLE_WORKER=
VERCEL_TOKEN=
VERCEL_PROJECT_ID=
VERCEL_TEAM_ID=
VERCEL_TEAM_SLUG=
LINEAR_API_KEY=
