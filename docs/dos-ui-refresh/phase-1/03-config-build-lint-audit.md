# Phase 1 — Formatting, build, lint, and workspace configuration audit (USA-206)

| Area | File(s) | Finding | Class | Impact | Disposition |
| --- | --- | --- | --- | --- | --- |
| Formatter | none (`.prettierrc*`, `biome.json`, `.editorconfig` absent) | No formatter; style is whatever the last agent wrote. `DosMvpAppClient.tsx` mixes Tailwind arbitrary values, inline styles, and a few `style` objects. | A/X | Diff noise and reviewer fatigue; agents "fix" formatting they did not intend to touch. | Phase 4 may add **Prettier with a check-only script** scoped to `app/dos/**` and `src/components/dos/**` — but not reformat existing files (that would be the "broad reformat" the project forbids). ?R only if Ryan wants repo-wide formatting. |
| Linter | none (`docs/ci-baseline.md`: skipped by decision, USA-93) | `next lint` was removed in Next 16; no ESLint config was ever added. `tooling/automation` also has none. | A/C (decided) | Unused imports, `any`, and hook-dependency mistakes are caught only by `tsc --strict`. | Keep as is for this project; optional `eslint-config-next` in Phase 7 as its own decision. |
| TypeScript | `tsconfig.json` | `strict: true` ✓. `target: "es5"` is stale (Next 16 ignores it for output but it widens `lib` checks); `include` lists `.next/types/**` and `.next/dev/types/**`, so `typecheck` behaves slightly differently before and after a build/dev run (`next-env.d.ts` imports `./.next/types/routes.d.ts`). CI runs typecheck **before** build and passes, so the dependency is benign today. | A/X (minor) | `next dev` rewrites `next-env.d.ts`; it showed as modified in the Phase 0 worktree after starting the dev server and had to be reverted before committing. Every agent that runs the dev server risks committing that churn. | Phase 7: note in README; do not change `tsconfig` in this project. |
| Build | `next.config.js` | Turbopack root pinned; host redirects; `serverActions.bodySizeLimit 10mb`; no `eslint`/`typescript.ignoreBuildErrors` overrides ✓. Build 49 s locally, ~2 min on Vercel. Logs "Supabase environment variables are not configured" during prerender when env is absent. | A/C | Fine. | Keep. |
| Styling config | `tailwind.config.js`, `postcss.config.js`, `app/globals.css` | Tailwind 3.4 with `usam.*` and `dos.*` tokens; `globals.css` is the **dark website theme** with global `:where(h1…h6)`, `p`, `li`, `input` color overrides that DOS neutralizes via `body:has(.dos-app-route)` and `!important`. Two design systems share one stylesheet. | A/X | Any DOS token added to `globals.css` leaks into the website and vice versa; light-background text goes invisible unless colored explicitly (known trap). | Phase 4: DOS tokens live in `tailwind.config.js` (`dos.*`) and a DOS-scoped stylesheet or the `app/dos/app/layout.tsx` block, never in `globals.css`. |
| Fonts | `app/layout.tsx` loads Inter + Oswald + Rajdhani from Google Fonts for the whole site; DOS layout re-declares Inter. `dos.html` wanted Barlow Condensed + JetBrains Mono. | A/C (DOS = Inter) | None; V10 also uses a system/Inter stack. | Spec: DOS font is Inter, no new fonts (matches `AGENTS.md`). |
| Package scripts | `package.json` | 71 scripts: `dev`, `build`, `start`, `typecheck`, `smoke`, and **66 `test:*`** regression scripts with **no aggregate `test` script**; `tooling/automation/docs/ci-baseline.md` nonetheless lists `npm test` as required. No `lint`, no `format`. No `"type": "module"` (the `test:preparation` loader emits an ESM re-parse warning). No `engines`, no `.nvmrc` (Node is pinned only in CI and Vercel). | A/X | Agents cannot "run the tests"; they must know which of 66 to run. Phase 0 had to construct the DOS subset by hand. | Phase 4 (USA-215): add `test:dos` (offline DOS subset) and `test:dos:preview` (needs URL) aggregate scripts — additive, safe. Do not add `engines` (Ryan decision; affects every worktree). |
| Regression scripts | `scripts/*.mjs` (66) | **32 scripts read `app/dos/app/DosMvpAppClient.tsx` as text; 24 anchor on `function <Name>` / `sliceBetween` string positions.** Example: `dos-readability` slices `function FieldLabel` … `function FieldInputClass`; `dos-field-contact-form` slices `function PersonFormContent` … `function DetailCard`. | **A/X — highest-impact finding for Phases 4–6** | Extracting any of those functions into `src/components/dos/` (the whole point of Phase 4) makes the scripts throw "Missing source section" even when behavior is identical. The one baseline failure (`dos-field-contact-form`) is already this failure mode. | Phase 4 rule: when a function moves, the script's *anchor path* is updated in the same PR and the assertion is kept verbatim; a PR that weakens an assertion must say so. Track in USA-215. |
| Duplicate enforcement | `docs/ci-baseline.md` vs `tooling/automation/docs/ci-baseline.md` | Two CI baseline docs with different required-check lists. | A/X | Confusion about what CI runs. | Phase 7: single source (safe). |
| Playwright | `playwright ^1.61.1` (website) vs `^1.62.0` (`tooling/automation`); no `playwright.config.*`; browsers installed per machine (1161–1228 present locally). | A/X (minor) | Two Playwright versions on one machine; CI installs Chromium on every run with a documented apt stall workaround. | Leave; note in Phase 8 verification plan. |
| CI | `.github/workflows/ci.yml` | Single job; required by branch protection; non-strict (stale-base merges allowed); `enforce_admins` off. Runs only three website contract scripts + homepage smoke. **No DOS script runs in CI.** | A/X | A DOS regression cannot fail CI. | Phase 4 (USA-215): add the offline `test:dos` aggregate as a CI step (additive; ~10 s). Strict status checks are a repo-settings change → Ryan (D9). |
| Hooks | none (`.husky`, `core.hooksPath` absent) | No pre-commit checks. | A/C | — | Keep. |
| Editor / workspace | `.claude/launch.json` (`dos-dev`), `.gitignore` (`.env*`, `*.zip`, `test-results/`, `*.tsbuildinfo`) | Fine. `.DS_Store` files still appear (`.claude/.DS_Store` untracked-ignored). | A/C | — | Keep. |
| Second package | `tooling/automation/package.json` (`type: module`, own `test`, own Playwright) | A separate Node project inside the repo with its own scripts and lockfile; not part of the website build. | A/C | None for UI. | Out of scope. |
| Costly / failing commands | `npm run build` 49 s; `npm run smoke` needs Chromium; `test:dos-field-contact-form` fails (stale). | — | — | Recorded in Phase 0. |
