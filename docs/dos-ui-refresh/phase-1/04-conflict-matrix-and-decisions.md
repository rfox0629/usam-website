# Phase 1 — Rules conflict matrix and decision list (USA-212)

## A. Conflict matrix

| # | Item | Source A | Source B | Nature of conflict | Impact on agents | Proposed disposition | Who decides |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C1 | DOS existence and scope | `AGENTS.md` ("USAM website, not the DOS app"; "Do not build Field UI yet") | `app/dos/README.md`, production `/dos` | Root instruction denies the product that exists. | Agents learn to ignore the root file; no reliable source of DOS rules. | Phase 3 spec becomes the DOS rule source; add a precedence line and a DOS section (or `app/dos/AGENTS.md`). `AGENTS.md` DOS statements marked superseded, not deleted, until Ryan confirms. | Safe to draft; **Ryan confirms wording** (D1). |
| C2 | Where DOS UI code lives | `app/dos/README.md` ("make DOS app UI fixes in `DosMvpAppClient.tsx` first") | Project description ("component foundation", "shared primitives"), `app/admin/README.md` ("shared UI should be generic primitives") | README rule produces the monolith the project must now refactor around. | Every PR conflicts in one file; regression scripts anchored on it break on extraction. | Spec supersedes: new shared primitives go to `src/components/dos/`; `DosMvpAppClient.tsx` adopts them incrementally; README sentence amended in the Phase 3 PR. | Safe (documentation + additive code). |
| C3 | Third-tab label | Production "More" | V10 frames "Apps" (V10 and USA-219 both say the rename is Ryan's) | None once read fully. | Agents may "align to the mockup" and rename. | Preserve "More". | **Ryan** (D2, already reserved). |
| C4 | Demo preview route | `app/dos/README.md` ("deprecated, redirects to `/dos`") | `app/dos/app/preview/page.tsx` (live behind a default token) | Doc vs code. | Agents may delete a route the project uses for screenshots, or promote it. | Keep for local/preview screenshots; decide production exposure. | **Ryan** (D3). |
| C5 | Legacy prototype API routes `app/api/dos/[collectiveSlug]/*` | README ("legacy helpers remain only for reference until fully removed") | Live handlers with only dead callers | Removing is an API surface change; keeping is dead code. | Agents may build on the wrong model. | Phase 7 manifest item; not touched by this project without approval. | **Ryan** (D4). |
| C6 | "Workspace" vs "Household" copy | `docs/architecture/workspace-terminology.md` (UI says "Household" for `workspace_id` scope) | DOS desktop sidebar "DOS WORKSPACE" and portal copy | Approved naming policy vs shipped copy. | Agents editing the sidebar will not know which word to keep. | Preserve current copy during the refresh; list as a copy decision. | **Ryan** (D5). |
| C7 | Groups V2 dual paths | `docs/dos-groups-v2-rollout.md` (flag, one workspace) | Client carries both paths | Two Groups UIs to refresh, one seen by a single workspace. | Phase 6 batch scope doubles or targets the wrong path. | Phase 2 inventories both; Phase 6 refreshes the default-off path unless Ryan promotes V2. | **Ryan** (D6). |
| C8 | Unmerged DOS UI branches (usa-163, usa-164, usa-138 prototypes) | Their commits restyle Journey/Home/People | This project's protections (Home unchanged; one source of truth) | Parallel direction that may or may not be intended. | Merging later would overwrite refreshed screens or redesign Home. | Treat as superseded by this project unless Ryan says otherwise; never merged or deleted by this project. | **Ryan** (D7). |
| C9 | Untracked `app/dos/library-preview/` in the main checkout | Unknown intent | USA-225 Library batch | Possible duplicate Library work. | Two Library directions. | Ask. | **Ryan** (D8). |
| C10 | Text-color ladder | `text-tokens.ts` / `tailwind dos.*` (USA-168: `#0F1520 / #3D4654 / #5A6473`, blue `#2450C8`) | Legacy palette in 3,500 literals (`#0F172A / #475569 / #64748B / #94A3B8`, blue `#2563EB` / `#1D4ED8`) | Two ladders and two blues in one file. | Agents pick whichever is nearest in the file. | Phase 3 spec picks one ladder and one blue; Phase 4 tokens implement it; V10 palette values are the tie-breaker. Not a product decision (readability guardrail already approved). | Safe; spec records the choice. |
| C11 | "Primary actions solid gold" | `AGENTS.md` UI rules | DOS blue primaries | Website/admin rule applied to DOS. | Gold buttons in DOS PRs. | Spec: two design contexts. | Safe. |
| C12 | "Use RLS for all tables" | `AGENTS.md` | DOS service-role data path (63 files) | Rule vs implementation. | False safety assumptions. | Spec + boundary map state reality; **no change** (auth is out of scope). | Safe (documentation only). |
| C13 | "Circles plan-only, do not automate" | `AGENTS.md` | `circle-scoring.ts` recommendations + human override | Half-conflict. | Agents may remove recommendations or auto-place. | Spec: recommendations deterministic and visible; placement human-only. | Safe. |
| C14 | Meetings structure | Production: calendar with history beneath, month/week "View" | Project description + V10: Calendar / Timeline toggle, no history under calendar, Timeline = logged past only with search, no status filters | Approved future vs current; not a rule conflict. | None if scoped in USA-218. | Implement in Phase 5 as approved. | Approved already. |
| C15 | My Record Overview | Production: "Today at a glance" + Recent Activity | Approved: Overview = Current then Recent; no Today at a glance, no summary dashboard, no Explore, no bottom arrows | Approved future vs current. | None if scoped in USA-220. | Implement in Phase 5 as approved. **"Current" rules are "Product logic — later" in V10** → what "Current" contains must be defined in the spec from existing data or left as today's content. | Spec proposes; **Ryan confirms if it changes data meaning** (D10). |
| C16 | Field/"Needs placement" bounded block | V10: bounded summary + sheet, "final behavior requires review" | Production: circle target on Home + Field filter | Explicitly unresolved by V10. | Agents may invent the sheet. | Spec lists as unresolved; Phase 6 People/Field batch preserves current behavior. | **Ryan** (D11) before USA-227. |
| C17 | CI baseline docs | `docs/ci-baseline.md` | `tooling/automation/docs/ci-baseline.md` (`npm test`) | Two lists. | Confusion. | Reconcile in Phase 7. | Safe. |
| C18 | SQL in `docs/` | `docs/usa-170-*.sql` | `supabase/migrations/` convention | Looks runnable. | Someone runs it. | Archive with a "never run" header in Phase 7. | Safe (move only). |
| C19 | Regression scripts anchored on function names | 24 scripts | Phase 4 extraction | Tests fail on pure moves. | Refactors blocked or tests weakened. | Rule: move anchors with the code, keep assertions verbatim, state any weakening. | Safe (process rule). |
| C20 | Merge authority | User instruction ("leave PRs ready unless repo instructions authorize auto-merge") | `tooling/automation/README.md`, `ENGINEERING_ONBOARDING.md` (no merge without founder authorization label) | Resolved: repo instructions do **not** authorize automatic merging. | — | **No PR in this project is merged by an agent.** PRs are stacked; each is left ready for Ryan. | Settled by repo rules. |

## B. Safe resolutions (no Ryan decision needed; executed in the phase named)

| Id | Resolution | Phase |
| --- | --- | --- |
| S1 | Write the canonical DOS spec with an explicit precedence statement: Linear project → canonical spec → `app/dos/README.md` → `AGENTS.md` (for `app/dos/**` only). | 3 |
| S2 | Amend `app/dos/README.md`: primitives live in `src/components/dos/`; `DosMvpAppClient.tsx` adopts them; correct the demo-route sentence once D3 is answered (until then, describe reality). | 3 (docs PR) |
| S3 | Choose one text ladder and one blue in the spec (token names from `text-tokens.ts`, values reconciled with V10). | 3 → 4 |
| S4 | Add `test:dos` (offline aggregate) and run it in CI; move script anchors with extracted code. | 4 (USA-215) |
| S5 | Consolidate the two shared-component roots (`components/dos/` and `src/components/dos/`) under one, via re-export shims, no behavior change. | 4 |
| S6 | Mark `docs/dos-groups-v2-polish-audit.md`, `-beta-validation.md`, `-member-portal-rollout.md`, release note, and `usa-191-review` as historical in a docs index; reconcile the two CI-baseline docs; archive `docs/*.sql`. | 7 |
| S7 | Record the stale `dos-field-contact-form` script as Obsolete; replace its intent with an assertion that matches the USA-168 form, in the same PR that first touches Person forms (never silently). | 5 (USA-217 Add Person) |
| S8 | Delete manifest candidates (evidence: zero importers, no route, no reference): `WorkspaceV2Shell.tsx`, `dos.html`, the four prototype clients, `src/lib/dos/{workspace,meetings,people}.ts`, merged remote branches `codex/dos-ui-blitz` and `clean/public-website-brand-refresh`. Deletion itself still needs founder review per `ENGINEERING_ONBOARDING.md` ("cleanup/deletion of old folders"). | 7 (manifest), Ryan approves execution |

## C. Ryan decision list

| Id | Question | Options | Consequence | Recommendation |
| --- | --- | --- | --- | --- |
| D1 | Confirm that `AGENTS.md`'s DOS statements ("not the DOS app", "do not build Field UI", website-only UI rules, "circles plan-only", engagement always editable) are superseded for `app/dos/**` by the Phase 3 canonical spec. | (a) Yes, add a precedence line + DOS section; (b) keep `AGENTS.md` as is and rely on the README. | (b) leaves agents reading a contradictory root file. | **(a)** |
| D2 | Rename "More" → "Apps"? | (a) keep "More"; (b) rename. | Copy change on the protected nav. | No recommendation; preserved until answered. |
| D3 | `/dos/app/preview` demo route in production. | (a) keep, documented, token-gated; (b) set `DOS_DISABLE_DEMO_PREVIEW=true` in production and keep it for local/preview; (c) delete. | (c) removes the only DB-free render path used for screenshots. | **(b)** — needs a Vercel env change (Ryan). |
| D4 | Delete legacy prototype API handlers `app/api/dos/[collectiveSlug]/*` in Phase 7? | (a) delete with their dead clients; (b) keep. | (a) is an API surface removal (unused). | (a), executed only with explicit approval. |
| D5 | Should DOS UI say "Household" where it currently says "Workspace", per `workspace-terminology.md`? | (a) yes, in the Phase 6 batch; (b) keep "Workspace"; (c) defer. | Copy change across sidebar/portal/settings. | (c) defer; not required for the refresh. |
| D6 | Groups V2 (`dos_groups_simplified_v2`) — promote to default for all workspaces before the Groups batch, or refresh both paths? | (a) promote (flag rows for every workspace — a data write, needs "Founder Approved — Production Data"); (b) refresh the default path only; (c) refresh both. | (a) is a data change; (c) doubles work. | **(b)** with V2 left visually as is; revisit after. |
| D7 | Unmerged DOS UI branches (`ryan/usa-163-journey-focused-revision`, `ryan/dos-journey-claude-ui-refresh`, `ryan/usa-164-people-home-v2-…`, `usa-138` prototypes). | (a) superseded by this project (leave unmerged, mark in Linear); (b) merge some first. | (b) would restyle Journey/Home ahead of the canonical spec; usa-164 touches protected Home. | **(a)** |
| D8 | What is `app/dos/library-preview/` (untracked in your main checkout)? Does it overlap USA-225? | — | Possible duplicate Library direction. | Please describe or commit/discard before USA-225. |
| D9 | Branch protection: enable strict status checks so a PR must be up to date with `main` before merge? | (a) yes; (b) no. | Repo settings change. | (a); low effort, prevents stale-base merges of stacked PRs. |
| D10 | My Record Overview "Current" section: the spec will define "Current" from existing data (active mentor relationship, open life-plan goals, latest journal/prayer entry). Confirm that reading is acceptable or supply the rule. | — | Defines what replaces "Today at a glance". | Spec proposal to review in Phase 3. |
| D11 | Field "Needs placement / recently added" bounded block (V10 §1) — approve the bounded summary + sheet pattern, or keep the current Home circle target + Field filter only? | (a) bounded summary + sheet; (b) keep current. | Phase 6 People/Field scope. | Decide before USA-227; Phase 5 unaffected. |

## D. Gate statement

Every item in the Phase 1 coverage list (CLAUDE.md, AGENTS.md, READMEs, all Markdown, `.github`, editor/workspace rules, package scripts, lint/format/TS/build config, design docs, old plans, migration notes, prompts, generated artifacts, duplicated/stale components, abandoned routes, experiments, feature flags, hard-coded UI values) is classified above or in the two companion documents. Nothing ambiguous was deleted or rewritten. Phase 2 (product and UI inventory) can start: it depends on none of D1–D11.
