# Phase 2 — Route, screen, and application-state matrix (USA-221)

Source: `app/dos/**`, `app/api/dos/**`, and `app/dos/app/DosMvpAppClient.tsx` at `de6862f` (382 top-level function components), plus the Phase 0 screenshots. "M" = mobile (< 768px), "D" = desktop (`md:` and up). Line numbers refer to the client file.

## 1. Routes and page-level states

| Route | Purpose | M | D | Auth / permission states (server-rendered before any UI) | Loading | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `/dos` | Portal: sign-in or workspace picker | ✓ | ✓ | unauthenticated → sign-in view; authorized + confirmed default → redirect `/dos/<slug>`; authorized without default → workspace list | `DosTargetLoader` (`app/dos/loading.tsx`) | `DosPortalClient.tsx` (357 lines, 74 hex) |
| `/dos/[slug]` | **The app** | ✓ | ✓ | unauthenticated → `/login?next=`; `configuration_error` → "DOS unavailable"; `unauthorized` → "Access pending"; `forbidden` → "Workspace unavailable"; `not_found` → "No workspace found"; slug alias → redirect to canonical slug | `DosTargetLoader` (`[collectiveSlug]/loading.tsx`) | Renders `DosMvpAppClient` with `DosAppData` |
| `/dos/app?workspace=` | Compatibility redirect | — | — | same five states, then redirect | `DosTargetLoader` | |
| `/dos/app/preview?demo=` | Demo (synthetic data) | ✓ | ✓ | wrong/missing token → "locked" screen; `DOS_DISABLE_DEMO_PREVIEW=true` → redirect `/dos` | — | Same client component |
| `/dos/setup`, `/dos/onboarding` | Personal workspace creation, USAM application | ✓ | ✓ | authenticated only | — | `DosSetupClient` (768 lines), `DosOnboardingClient` (2,916 lines) — **not in V10** |
| `/dos/workspaces/[slug]`, `/dos/admin`, `/dos/[slug]/{meetings,people,people/[id],meetings/[id]}` | Redirects | — | — | — | — | Keep redirects |
| `/dos/library/marriage-assessment`, `/dos/library/remnant` | Standalone library pages | ✓ | ✓ | (public/token as implemented) | — | `MarriageAssessmentClient`, `RemnantCollectionClient` |
| `/dos/book/[token]` | Public booking form | ✓ | ✓ | token | — | `DosTableBookingForm` — not in V10 |
| `/dos/review/[token]`, `/dos/review-options/[token]`, `/dos/testimony/[token]` | Public review / options / testimony forms | ✓ | ✓ | token; own OG image | — | Not in V10 |
| `/groups/[slug]` (outside `/dos`) | Public group home / member portal | ✓ | ✓ | public + lightweight member session | — | `app/groups/GroupJourneyView.tsx` shares `GuidedJourneyUi` and `VoiceTextarea` with the app |

## 2. In-app screens and subviews (client state, not routes)

Primary state: `ActiveTab` = home | meetings | people | more; `MoreAppView` (15 values). Deep links: `?view=<MoreAppView>`, `?person=<id>`, `?openGroup=<id>`, `?tab=growth|commitments`, `?resource=<slug>`, `?walkthrough=usam`.

| Screen / subview | Where | M | D | Components (line) | Reachable states | V10 coverage |
| --- | --- | --- | --- | --- | --- | --- |
| **Home** | `activeTab=home` | ✓ | ✓ (as Dashboard) | `CircleFocusHero` (19670) circle target, Notifications, Log Meeting + Schedule / Add Person / Accountability, Today's Alignment (`Open My Record`), activity rows; desktop `DesktopHomeDashboard` (15012): Notifications, Today's Alignment, Top Time Investments, Accountability, Assigned Resources, Upcoming, Recent Fruit, Recent Reviews, Table Activity; sheets Upcoming (46525), Activity (46550), Scheduling Links (46564); USAM walkthrough (46482, `?walkthrough=usam`) | populated; "No notifications today."; "Nothing upcoming."; walkthrough stepper | **Out of scope by decision (protected)** |
| **Meetings** | `activeTab=meetings` | ✓ | ✓ | `TabHero`, `MobileSectionSearch` / `DesktopSectionSearch`, `MeetingCalendarView` (20650) with `meetingCalendarViewTabs` month/week (16736), Needs Logging list, Recently Logged list, calendar-view popover (20545, `role=dialog`), Google event detail sheet (21092), `MeetingDetailOverlay` (38253), Meeting options sheet (38669), notes editor sheet (38138, editable), send-confirmation sheet, Log/Edit Meeting `DosWorkflowPage` (46762 editable), `ScheduleMeetingForm` (22915) | month / week; needs-logging empty ("All caught up · No meetings need logging."); "No meetings yet"; day selected; Google source connected/disconnected/syncing; meeting detail: not sent / pending / submitted / approved / private review status; scheduled vs logged | Calendar ✓, Timeline ✓ (**does not exist today**), day sheet (demo), **meeting detail record: not drawn** |
| **Field (People)** | `activeTab=people` (highlights the More tab) | ✓ | ✓ | `TabHero`, Settings, `PeopleCircleTabs` all/3/12/70/120 (16892), `MobileSectionSearch`, `CircleLayerList` (34604), `DesktopPeopleIndex` table (19126), `EmptyState` ×3, `CircleLayerSheet` (34651), `CirclesDetailOverlay`, `PeopleImportSheet` (27485 editable), Add/Edit Person `DosWorkflowPage` (`PersonFormContent`, editable) | populated; "No people found."; "No field added yet."; "No primary field contacts."; import in progress | Field All + Needs Placement ✓ (**Needs Placement sheet is "interaction demonstration"**, D11) |
| **Person Record** | `PersonDetailOverlay` (36007), `?person=` | ✓ | ✓ | tabs Overview / Timeline / Details (`PersonDetailTab`); sections Last conversation, Next, Walking together, Accountability, Prayer, Feedback, Relationship brief; sheets: Circle Suggestion, "DOS noticed something", Add, Request feedback (37569), person sheet (37614), accountability detail/edit/check-in/progress (14340–14525), commitment form/subject/success, resource assignment form/check-in/duplicate/success, `LeaderJourneyProgressSheet`, `PersonFeedbackDetailSheet` | populated; no meetings; no accountability; engagement shown only when flag on; circle suggestion pending; "No timeline activity yet."; "No contact yet" | Overview ✓; **Timeline and Details tabs: not drawn** |
| **More / Apps launcher** | `activeTab=more`, view `apps`/null | ✓ | ✓ (`DesktopMoreLauncher` 15431) | `AppsCatalogSection` grid (7739): sections installed / missionary / coming_soon; search (`Search`) | populated; "No items found." | Apps ✓ (three scroll positions) |
| **Settings** | view `settings` | ✓ | ✓ | `DesktopSettingsProfileView` (7248), `AdvancedFeaturesPanel` (7198), `ProfileSheet` / `EditProfileSheet` (editable), `AvailabilityEditSheet` (17509), invitation editor (18169) with sections availability / calendar / hosts / overview / questions / rules / sharing | flag on/off; Google calendar connected/disconnected | **Not drawn** |
| **My Record** | view `my_record` | ✓ | ✓ | `MyRecordWorkspace` (33041), `MyRecordTabBar` + `myRecordTabs` Overview / Walk / Growth / Purpose / Faithfulness (16806); 13 internal `MyRecordTab` values; sheet modes view / edit / new (`MyRecordSheetMode`); forms Journal, Prayer, Mentor relationship, Mentor meeting, Prophetic word, Life plan, Learning book, Learning chapter, Assessment, External assessment (28989–30829); Timeline sheet; assessment library/detail panels; report panel; `MyRecordContextualFloatingActions` | Overview: Today at a glance + Recent activity; per-tab empty rows ("No prophetic words recorded yet.", "No chapter notes yet.", "No summary added yet."); timer running; attachments | Rail + all five tabs + record detail ✓; **entry forms for Reflection, Prophetic word, Faithfulness, Mentor meeting, Book: not drawn**; Time with God form ✓ |
| **Groups** | view `groups`, `?openGroup=` | ✓ | ✓ | `GroupsWorkspace` (9387) list all/mine (16686), `GroupDetailWorkspace` (9606) with `GroupDetailTabBar` (10583): overview / members / gatherings / attendance / people / prayer / journeys / resources / settings; sheets Create (11721), Settings (11913), Invite (11483), Gathering form (12306), Journey edit/assign (9119/9183), Assign target picker (9308), Leader journey progress (6573), `MemberGroupHomePreview`; workflow panels attendance / notes / prayer | "No groups yet." ×3; V2 flag on/off (two paths); join requests pending; attendance present/absent/guest; "No primary leader yet" | **Not drawn** |
| **Prayer** | view `prayer` | ✓ | ✓ | `MobilePrayerWorkspace` (26822) / `DesktopPrayerWorkspace` (26987); `prayerWorkspaceTabs` prayers / prayer_team / answered (16791); `prayerRequestViewTabs` praying / answered; filters all / group / high_priority / needs_follow_up / private / public; sheets Add partner (25676), Add request (25821), Request detail (25939), Edit request, Partner detail (26339), Log prayer (26605), Prayer detail (26713), Prayer resources library/detail (34939/35060) | "No prayer requests yet."; "No prayer partners yet."; answered; follow-up overdue | List ✓ (visual only), Add request ✓ (visual only); **Prayer team and Answered tabs: not drawn** |
| **Fruit** | view `fruit` | ✓ | ✓ | `fruitViewTabs` activity / forms / impact / reviews (16754); `RecentFruitStoryCard`, `MultiplicationTreeTeaser`, `FruitSnapshotSourceSection`, `FruitFormsGrid`, `FruitBreakdownSection`, `SubmittedReviewsList`, `DesktopFruitStoriesTable`; sheets Fruit activity detail (24042), Fruit form preview (24251), Review detail (33764), Record Fruit (46846) | "No fruit activity yet."; "No reviews yet."; forms live / coming_soon | **Not drawn** |
| **Library** | view `library`, `?resource=` | ✓ | ✓ | `LibraryResourceViewState` null / resource / prayer_collection / prayer_detail / remnant; `LibrarySection` ×6, `CatalogResourceList`, `LibraryResourceShell`, `LibraryCatalogResourcePage`, `RemnantLibraryResourceContent`, `PrayerResourcesLibraryContent`, `GuidedResourceDetailSheet` (6214), `ResourcePickerSheet` (35454); `GuidedJourneyUi` steps | index; resource overview; focused step; "Resource not found."; assign flow (individual / group) with duplicate notice | Reading-plan overview + focused step ✓, Assessment ✓; **Library index: not drawn**; **assessment in-progress/results: not drawn** |
| **Testimony Practice** (`in_season`), **Reports**, **Stewardship**, **Table Flow** | views | ✓ | ✓ | `TabPageHeader` + `TabHero` + placeholder copy ("Coming soon.", "Stewardship is not installed yet.") | placeholder only | Apps tiles only |
| **Missionary Profile**, **Prayer Team**, **Support Team**, **Organizations** (USAM layer) | views | ✓ | ✓ | `OrganizationStatusCard`, `OrganizationConnectionRow`, `DesktopOrganizationsView`, `UsamApplicationSheet` (15718) | not connected / applied / connected | **Not drawn** (USAM network layer) |
| **Quick actions** | FAB (34424) | ✓ | ✓ | `MobileFloatingActions` ×2; items per tab (44629–44670): Meetings → Log / Schedule; People → Add Person; Prayer → add; Fruit → record; More → contextual; Home → Log / Schedule | open / closed; suppressed on My Record and Library (44676) | Extended FAB per screen ✓ (V10 removes the "+" on Apps) |

## 3. Cross-cutting states

| State class | Implementation | Evidence |
| --- | --- | --- |
| Loading (route) | `DosTargetLoader` skeleton at `/dos`, `/dos/[slug]`, `/dos/app` | `loading.tsx` ×3 |
| Loading (in-app) | `isSubmitting` ×243 sites → button label swaps ("Scheduling…", "Adding…"), no global spinner | grep |
| Empty | `EmptyState` (4739), `SectionEmptyState`, `MyRecordCompactEmptyRow`; ~25 distinct copy strings ("No groups yet.", "Nothing upcoming.", "All caught up", …) | grep |
| Success | Saved sheet (13605), `CommitmentSuccessSheet`, `ResourceAssignmentSuccessSheet`, "Study Assigned" sheet, `MeetingSendConfirmationSheet` | sheet list |
| Error | inline `setError` ×143 / `errorMessage` ×159 rendered next to the form; **no toast, no `alert()`**; API errors surface as sentences from `NextResponse.json({ error })` | grep |
| Confirmation | `DiscardChangesDialog` (16000) via `useUnsavedWorkGuard` on 23 editable sheets + 4 workflow pages; `confirm()` ×6 for destructive actions | Phase 0 |
| Destructive | delete meeting / person / prayer request / partner / reminder / fruit event; remove group member; Google disconnect | boundary map §10 |
| Offline / retry | **None.** `navigator.onLine` 0 uses; "retry" 2 mentions; a failed fetch shows the inline error and keeps the form | grep |
| Permission (in-app) | admin vs member (`DosAuthorizedUser.access`), shared leadership (`identity.ts`), USAM workspace gating for Kitchen Table Gospel / Four Questions flows and network-layer views, Advanced Feature flags | boundary map |
| Demo | locked screen; fixture data with relative dates | preview page |

## 4. Overlays, sheets, dialogs, drawers, editable surfaces

- **Primitives:** `Sheet` (16160, `fixed inset-0 z-[1000]`, blur backdrop) ×60 instances; `MobileBottomSheet` (16277, `absolute z-[80]`) ×2; `ProfileSheetFrame` (16405, `absolute z-[90]`); `DosWorkflowPage` (15943, `fixed inset-0 z-[120]`, desktop offset `md:left-[232px] xl:left-[260px]`) ×4; `DiscardChangesDialog` (`z-[1100]`); calendar-view popover (`fixed z-[90]`); `PersonDetailOverlay`, `MeetingDetailOverlay`, `CirclesDetailOverlay` (full-screen overlays inside the scroll container).
- **Editable (23 declared):** Edit Journey, Add to group, New Group, Edit group, Gathering form, Assign/Edit Journey, Resource check-in, New/Edit Accountability ×2, Edit Accountability, Check in, Add progress, Add Person (sheet variant), Log Check-In, Add Prayer Partner, Add Prayer Request, Edit Prayer Request, Log Prayer, Import Contacts, Add/Edit Notes, Add/Edit Reminder or Prayer Request, Log/Edit Meeting; plus the 4 workflow pages (Log Meeting, Schedule Meeting, Add Person, Edit Person) and the 10 My Record forms inside `MyRecordSheetFrame`.
- **Inspection (default, ~37):** person/partner/request/resource/fruit/review details, More actions, Saved/Assigned notices, Circle Suggestion, "DOS noticed something", walkthrough, Upcoming, Activity, Scheduling Links, Send Resource, Prayer Resources, Meeting options, Record Fruit, Apply to USA Missionaries, availability/invitation editors (**these two hold user input but are not declared editable** — flag for USA-211).

## 5. Fixed navigation, safe areas, scroll and overflow

| Item | Current value | V10 target | Risk |
| --- | --- | --- | --- |
| Bottom nav | `absolute inset-x-0 bottom-0 z-[60]`, `bg-white/62 backdrop-blur-2xl`, `pb-[calc(env(safe-area-inset-bottom)+0.55rem)]`, `md:hidden` | solid `#FFFFFF`, no backdrop filter, z 30, hairline + restrained shadow | Content shows through (screenshots) |
| Content clearance | scroll container `pb-28` (112 px) or `pb-40` (160 px on More) (44757) | 134 px reserved on every scrollable screen (nav 70 + gap 14 + safe 34 + 16) | Last rows can sit under the bar on non-More tabs |
| FAB | `fixed bottom-[calc(env(safe-area-inset-bottom)+5rem)] right-[max(1rem,calc((100vw-430px)/2+1rem))]` (28893) | stacks beneath the nav (z 10 vs 30) | FAB overlaps last row and nav corner |
| Forms | `DosWorkflowPage` hides the nav (z 120 over everything); sticky footer `bg-white/97 backdrop-blur-sm` (4916) | forms hide the nav; sticky primary above keyboard; never both a sticky bar and the nav | Footer translucency |
| Sheets | `Sheet` full-screen `z-[1000]` with blur; `MobileBottomSheet` bottom-anchored | 20 px dialog for discard; bottom sheets for day/placement | Three variants |
| Horizontal overflow | `overflow-x-hidden` on the app scroll container; `overflow-x-auto` ×7 (tables); `min-w-[820px]` ×1 (desktop table), `min-w-[150px]/[94px]/[74px]`; `100vw` ×5; `truncate` ×204; `whitespace-nowrap` ×7 | no horizontal page scroll; tables scroll inside their own container | The 820 px table must stay inside `overflow-x-auto`; 100vw uses need checking at 390 px |
| Z-index ladder | 10 / 20 (sticky) · 30 · 40 · 60 (nav) · 80 · 90 · 92 · 120 · 1000 · 1100 | 10 (FAB) · 30 (nav) · sheets above | Ten distinct layers with no documented order |
| Safe-area | `env(safe-area-inset-bottom)` used in nav, FAB, sheets, workflow page, footers; `env(safe-area-inset-top)` in calendar popover and third sheet; `env(safe-area-inset-left/right)` in calendar popover | | Missing on the scroll container itself |
| Desktop | Sidebar `md:left-[232px] xl:left-[260px]`; `md:hidden` ×25 / `hidden md:*` ×6; no `matchMedia`, layout is CSS-only | **V10 draws no desktop frames** | Desktop refresh has no mockup |

## 6. Screens absent from V10 (must not be guessed)

Home (protected, out of scope) · Dashboard and every desktop layout · Portal / sign-in / workspace picker · Setup and onboarding (`DosSetupClient`, `DosOnboardingClient`) · Settings, profile, availability and invitation editors · Person Timeline and Details tabs · Meeting detail record · Meeting notes editor, send-confirmation, meeting options · Groups (list, detail with 9 tabs, 7 sheets) · Fruit (4 views, 4 sheets) · Library index, resource picker, assign flow, Remnant, prayer resources · Prayer team and Answered tabs, partner/request detail, log prayer · My Record entry forms other than Time with God (Reflection, Prophetic word, Faithfulness, Mentor meeting, Book), assessments in-progress/results, timeline sheet · Reports / Stewardship / Testimony Practice placeholders · USAM network layer (Missionary Profile, Prayer Team, Support Team, Organizations, USAM application) · Public token forms (book, review, review-options, testimony) · Public group portal · Circle Suggestion and "DOS noticed something" sheets · CSV import · Walkthrough · Demo lock screen · Blocked/permission screens.

Also explicitly **not drawn by V10's own list:** Person Timeline & Details tabs, meeting detail record, Groups, Fruit, Library index, Settings, Prayer team & Answered tabs, assessment in-progress and results, entry forms for Reflection, Prophetic word, Faithfulness, Mentor meeting, Book.
