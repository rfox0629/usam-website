import { chromium } from "playwright";
const BASE = "http://localhost:3100";
const b = await chromium.launch({ executablePath: "/opt/pw-browsers/chromium" });
const page = await (await b.newContext({ viewport: { width: 1440, height: 900 } })).newPage();
await page.goto(`${BASE}/login?next=%2Foperations%2Fsubmissions`);
await page.fill('input[name="email"]', "reviewer.usa289@localtest.dev");
await page.fill('input[name="password"]', "Local-Test-Pass-1");
await page.getByRole("button", { name: "Sign In" }).click();
await page.waitForURL(/operations\/submissions/);
await page.locator('a[href*="/dos-access/"]', { hasText: "Quinn" }).first().click();
await page.getByRole("button", { name: "Approve and set up access" }).click();
await page.waitForURL(/(saved|error)=/);
console.log("approve:", decodeURIComponent(page.url().split("?")[1]));
await b.close();
const b2 = await chromium.launch({ executablePath: "/opt/pw-browsers/chromium" });
const p2 = await (await b2.newContext({ viewport: { width: 390, height: 844 }, isMobile: true, deviceScaleFactor: 2 })).newPage();
await fetch("http://127.0.0.1:54321/auth/v1/__password", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ email: "quinn.usa289@localtest.dev", password: "Local-Test-Pass-1" }) });
await p2.goto(`${BASE}/login?next=%2Fdos%2Fquinn-synthetic`);
await p2.fill('input[name="email"]', "quinn.usa289@localtest.dev");
await p2.fill('input[name="password"]', "Local-Test-Pass-1");
await p2.getByRole("button", { name: "Sign In", exact: true }).click();
await p2.waitForURL(/dos\/quinn-synthetic/);
await p2.waitForTimeout(4000);
await p2.screenshot({ path: "/home/user/usam-website/docs/dos-onboarding/evidence/33-mobile-new-user-first-open.png" });
console.log("opened", p2.url());
await b2.close();
